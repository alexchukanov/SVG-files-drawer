<Page
    x:Class="Svg.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Svg"
    xmlns:model="using:Svg.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="layoutRoot">
        <Canvas Name="canvas" Width="{x:Bind ViewModel.WinWidth}" Height="{x:Bind ViewModel.WinHeigth}" />

        <!--ItemsControl ItemsSource="{x:Bind ViewModel.ShapeInfoList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding Left}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Top}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="model:ShapeInfo">
                    <TextBlock Text="{x:Bind Name}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->
        
    </Grid>
</Page>
